# History Tasks

## 历史与撤销模块任务

- [ ] HI-01 实现应用数据目录 `history.sqlite` 路径解析（依赖：CM-11）
- [ ] HI-02 创建 `batches` 表结构（依赖：HI-01）
- [ ] HI-03 创建 `file_results` 表结构（依赖：HI-02）
- [ ] HI-04 创建 `candidates` 表结构（依赖：HI-03）
- [ ] HI-05 创建 `rule_details` 表结构（依赖：HI-04）
- [ ] HI-06 创建 `undo_records` 表结构（依赖：HI-03）
- [ ] HI-07 创建 `settings_snapshots` 表结构（依赖：HI-02, SE-13）
- [ ] HI-08 实现 SQLite 初始化和 schema 版本记录（依赖：HI-02 至 HI-07）
- [ ] HI-09 实现批次创建记录写入（依赖：HI-08）
- [ ] HI-10 实现文件处理结果写入，包含状态、源指纹、输出路径和失败原因（依赖：HI-09）
- [ ] HI-11 实现候选标题、分类分数和规则明细写入（依赖：HI-04, HI-05, HI-10）
- [ ] HI-12 实现设置快照摘要写入（依赖：HI-07, SE-13）
- [ ] HI-13 实现批次列表分页查询（依赖：HI-09）
- [ ] HI-14 实现批次详情查询（依赖：HI-10, HI-11）
- [ ] HI-15 实现只针对已自动输出和手动确认输出记录的重复检测查询（依赖：HI-10）
- [ ] HI-16 暴露重复检测接口供 `ingest` 调用（依赖：HI-15）
- [ ] HI-17 实现输出副本创建时撤销记录写入，保存创建时元数据和哈希（依赖：HI-06, RN-11）
- [ ] HI-18 实现撤销前输出副本不存在检查（依赖：HI-17）
- [ ] HI-19 实现撤销前大小和修改时间比对（依赖：HI-17）
- [ ] HI-20 实现元数据不一致时内容哈希比对（依赖：HI-19）
- [ ] HI-21 实现未改动输出副本删除和撤销状态更新（依赖：HI-18 至 HI-20）
- [ ] HI-22 实现已改动输出副本默认跳过和风险提示记录（依赖：HI-20）
- [ ] HI-23 添加历史写入、详情查询、重复检测、撤销缺失、撤销改动和撤销成功测试（依赖：HI-01 至 HI-22）
